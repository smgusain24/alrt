[build]
builder = "dockerfile"
dockerfilePath = "apps/workers/Dockerfile"

[deploy]
startCommand = "uv run --package alrt-workers celery -A alrt_workers.celery_app worker --loglevel=info"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
